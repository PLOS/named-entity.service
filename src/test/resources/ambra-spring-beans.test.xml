<?xml version="1.0" encoding="UTF-8"?>

<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:aop="http://www.springframework.org/schema/aop"
       xmlns:tx="http://www.springframework.org/schema/tx"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
                           http://www.springframework.org/schema/tx http://www.springframework.org/schema/tx/spring-tx-3.2.xsd
                           http://www.springframework.org/schema/aop http://www.springframework.org/schema/aop/spring-aop-3.2.xsd">


    <!-- use tomcat's 7.x jdbc connection pool -->
    <bean id="ambraDataSource" class="org.apache.tomcat.jdbc.pool.DataSource" destroy-method="close" >
       <property name="url" value="${ambradb.url}" />
       <property name="driverClassName" value="${ambradb.driver}" />
       <property name="username" value="${ambradb.username}" />
       <property name="password" value="${ambradb.password}" />
    </bean>

    <bean id="ambraService" class="org.plos.namedentity.service.AmbraServiceImpl">
        <property name="userRegistrationService" ref="userRegistrationService" />
        <property name="namedEntityDBService" ref="namedEntityDBService" />
        <property name="userService" ref="userService" />
    </bean>
</beans>
